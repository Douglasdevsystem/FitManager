{
  "name": "fitmanager-functions",
  "private": true,
  "description": "FitManager - Cloud Functions (notificações, vencimentos, geração de embeddings)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.65",
    "face-api.js": "^0.22.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1"
  }
}
